// app/context/SidebarContext.tsx"use client";import React, { createContext, useContext, useState } from "react";type ShowPageState = {  tickePage: boolean;  stats: boolean;};type SidebarContextType = {  showPage: ShowPageState;  setShowPage: React.Dispatch<React.SetStateAction<ShowPageState>>;};const SidebarContext = createContext<SidebarContextType | undefined>(undefined);export function SidebarProvider({ children }: { children: React.ReactNode }) {  const [showPage, setShowPage] = useState<ShowPageState>({    tickePage: false,    stats: false,  });  return (    <SidebarContext.Provider value={{ showPage, setShowPage }}>      {children}    </SidebarContext.Provider>  );}export function useSidebarContext() {  const context = useContext(SidebarContext);  if (!context) {    throw new Error("useSidebarContext must be used inside SidebarProvider");  }  return context;}
